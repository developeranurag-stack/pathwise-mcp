# Must point at the SAME Neon Postgres database as pathwise/.env — copy the
# value from there, don't invent a separate database.
DATABASE_URL=postgresql://user:password@ep-example-12345.region.aws.neon.tech/pathwise?sslmode=require
